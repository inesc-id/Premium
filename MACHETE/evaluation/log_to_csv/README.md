# Evaluation Metrics extractor

The python script extracts the log file values into a csv file.

Run extractor to csv:

```
extract_log_to_csv.py <number_of_experiments> <number_of_nodes> <filename>
```

Example:
```
extract_log_to_csv.py 27 2 machete_log.txt
```
